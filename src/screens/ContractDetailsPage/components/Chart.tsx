import React from "react";

export const Chart: React.FC = () => {
    return (
        <div className="min-h-[360px] bg-theme flex items-center justify-center rounded-lg border-b border-gray-300">
            <span className="text-theme">Chart space (empty)</span>
        </div>
    );
};
